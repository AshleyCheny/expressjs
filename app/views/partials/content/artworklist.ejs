<article class="sidebar">
  <h1 class="sidebar-title">Artwork on display</h1>
  <p class="sidebar-body">While you attend the conference, head over to our gallery where yo can check out some of the work from our speakers.</p>
  <div class="pixgrid clearfix">
    <!-- everything uses ejs should be wrapped with <%= %> -->
    <% if( artwork.length > 0 ) { %>
    <%  for( i=0; i < artwork.length; i++ ){ %>
        <img src="/images/artwork/<%= artwork[i] %>" alt="Artwork <%= i %>">
      <% } %>
    <% } %>
  </div>
</article>
